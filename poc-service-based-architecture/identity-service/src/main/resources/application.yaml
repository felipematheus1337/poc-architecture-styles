server:
  port: 8082

spring:
  application:
    name: identity-service

  datasource:
    url: jdbc:postgresql://localhost:5432/sba
    username: admin
    password: admin123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: identity
        format_sql: true
    show-sql: true

  sql:
    init:
      mode: never

  jackson:
    serialization:
      indent_output: true

logging:
  level:
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG