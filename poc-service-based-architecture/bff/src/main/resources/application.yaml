server:
  port: 8081

spring:
  application:
    name: sba-bff
  cloud:
    openfeign.client.config:
      default:
        connectTimeout: 30000
        readTimeout: 120000


services:
  order:
    url: http://localhost:8083/api/v1/orders
  identity:
    url: http://localhost:8082/api/v1/identity


loggin.level:
  root: WARN